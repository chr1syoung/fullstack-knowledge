# 16-AI 前端面经专题

> AI 应用场景下的前端技术面试题
> 最后更新：2026-03-31

---

## 16.1 一面：技术基础

### 16.1.1 SSE 与 WebSocket 的区别

#### 知识点详解

**SSE（Server-Sent Events）：**

```javascript
// 客户端建立 SSE 连接
const eventSource = new EventSource('/api/stream');
eventSource.onmessage = (event) => {
    console.log('收到数据:', event.data);
};
// 浏览器自动重连
```

**WebSocket：**

```javascript
const ws = new WebSocket('wss://example.com/ws');
ws.onopen = () => ws.send(JSON.stringify({ message: '你好' }));
ws.onmessage = (event) => console.log('收到:', event.data);
// 需要手动实现重连
```

**核心对比：**

| 特性 | SSE | WebSocket |
|------|-----|-----------|
| 通信方向 | 服务器→客户端单向 | 全双工双向 |
| 协议 | HTTP（text/event-stream） | WebSocket（ws/wss） |
| 自动重连 | ✅ 浏览器原生 | ❌ 需手动 |
| 二进制数据 | ❌ 仅文本 | ✅ 支持 |
| 防火墙穿透 | 易 | 可能受限 |

#### 真实面试题

**题目：SSE 和 WebSocket 的区别？AI 聊天为何选 SSE？**

**满分答案：**

**SSE 与 WebSocket 区别：**

1. **通信方向**：SSE 单向（服务器推送），WebSocket 全双工
2. **协议**：SSE 基于 HTTP，WebSocket 独立协议
3. **自动重连**：SSE 原生支持，WebSocket 需手动实现
4. **数据类型**：SSE 仅文本，WebSocket 支持二进制

**AI 聊天选 SSE 的原因：**

1. **单向通信足够**：AI 回复是服务器推送，无需客户端主动发消息
2. **基于 HTTP**：易穿透防火墙，兼容性好
3. **自动重连**：长会话友好
4. **实现简单**：

```javascript
// SSE 实现 AI 流式聊天
const source = new EventSource('/api/chat?message=你好');
source.onmessage = (e) => appendText(e.data);
```

---

### 16.1.2 Markdown 代码块高亮渲染性能

#### 知识点详解

**优化策略：**

1. **虚拟滚动**：只渲染可视区域（react-virtuoso）
2. **Web Worker 后台高亮**：不阻塞主线程
3. **增量高亮**：只处理新增内容
4. **requestIdleCallback**：空闲时执行
5. **LRU 缓存**：复用高亮结果

```typescript
// Web Worker 高亮
const worker = new Worker('./highlight.worker.ts', { type: 'module' });
worker.postMessage({ code, lang });
worker.onmessage = (e) => updateBlock(e.data.highlighted);

// 增量高亮：检测代码块是否完整
function shouldHighlight(code: string): boolean {
    return (code.match(/```/g) || []).length % 2 === 0;
}
```

#### 真实面试题

**题目：如何保证 Markdown 代码块的高亮渲染性能？**

**满分答案：**

**5 层优化策略：**

1. **虚拟滚动**：只渲染可视区域，长对话不卡顿
2. **Web Worker 后台高亮**：不阻塞主线程
3. **增量高亮**：只处理新增内容，避免全量重渲染
4. **requestIdleCallback**：空闲时执行高亮
5. **LRU 缓存**：相同代码复用结果

---

### 16.1.3 Record 与 Partial

#### 知识点详解

```typescript
// Record<K, T>：键为 K，值为 T
type UserMap = Record<string, User>;
type StatusMap = Record<'success' | 'error', number>;

// Partial<T>：所有属性变为可选
type PartialUser = Partial<User>;

// AI 接口应用
interface AIResponse<T = Record<string, unknown>> {
    code: number;
    data: T;
    usage?: Record<string, number>;  // Token 使用统计
}

type ConfigUpdate = Partial<ModelConfig>;  // 配置部分更新
```

#### 真实面试题

**题目：TypeScript Record 和 Partial 的作用？AI 接口如何用？**

**满分答案：**

**Record<K, T>**：构造对象类型，键为 K，值为 T

```typescript
type ModelResponses = Record<'openai' | 'claude' | 'gemini', AIResponse>;
```

**Partial<T>**：将所有属性变为可选

```typescript
type ConfigUpdate = Partial<ModelConfig>;
function updateConfig(current: ModelConfig, updates: ConfigUpdate) {
    return { ...current, ...updates };
}
```

**AI 接口应用：**
- `Record` 管理多模型响应
- `Partial` 实现配置部分更新
- `Record<string, number>` 统计 Token

---

### 16.1.4 流式字符串按句子切割

#### 知识点详解

```typescript
function* sentenceGenerator(stream: string): Generator<string> {
    const sentenceEndings = /[.!?。！？]+/g;
    let buffer = '';
    
    for (const char of stream) {
        buffer += char;
        if (sentenceEndings.test(char)) {
            const sentence = buffer.trim();
            if (sentence) yield sentence;
            buffer = '';
        }
    }
    if (buffer.trim()) yield buffer.trim();
}

// 使用
for (const sentence of sentenceGenerator("Hello. 你好！")) {
    console.log(sentence);
}
```

#### 真实面试题

**题目：手写函数按句子切割流式字符串**

**满分答案：**

```typescript
function* sentenceGenerator(stream: string): Generator<string> {
    const sentenceEndings = /[.!?。！？]+/g;
    let buffer = '';
    
    for (const char of stream) {
        buffer += char;
        if (sentenceEndings.test(char)) {
            yield buffer.trim();
            buffer = '';
        }
    }
    if (buffer.trim()) yield buffer.trim();
}
```

---

### 16.1.5 打字机光标闪烁效果

#### 知识点详解

```css
.typing-cursor {
    display: inline-block;
    width: 2px;
    height: 1.2em;
    background-color: currentColor;
    animation: blink 1s step-end infinite;
    vertical-align: text-bottom;
}

@keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
}

/* ChatGPT 风格方块光标 */
.chatgpt-cursor {
    width: 8px;
    border-radius: 1px;
}
```

#### 真实面试题

**题目：CSS 实现 ChatGPT 打字机光标效果**

**满分答案：**

```css
.typing-cursor {
    display: inline-block;
    width: 2px;
    height: 1.2em;
    background-color: currentColor;  /* 继承文字颜色 */
    animation: blink 1s step-end infinite;
    vertical-align: text-bottom;
}

@keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
}
```

**关键点：**
- `step-end`：跳跃式闪烁
- `currentColor`：自动适应主题
- `vertical-align: text-bottom`：对齐文字底部

---

### 16.1.6 Promise.all vs Promise.allSettled

#### 知识点详解

| 特性 | Promise.all | Promise.allSettled |
|------|-------------|-------------------|
| 成功条件 | 所有成功 | 所有完成 |
| 失败行为 | 立即 reject | 等待全部 |
| 返回值 | 结果数组 | 状态对象数组 |

```typescript
// 多模型对比 → allSettled
const results = await Promise.allSettled([
    callOpenAI(prompt),
    callClaude(prompt),
    callGemini(prompt)
]);
results.forEach((r, i) => {
    if (r.status === 'fulfilled') useResponse(i, r.value);
    else showError(i, r.reason);
});
```

#### 真实面试题

**题目：Promise.all 和 Promise.allSettled 的区别？AI 模型调用选哪个？**

**满分答案：**

**区别：**

| 特性 | Promise.all | Promise.allSettled |
|------|-------------|-------------------|
| 成功条件 | 全部成功 | 全部完成 |
| 失败行为 | 立即 reject | 等待全部完成 |
| 返回值 | 结果数组 | `{status, value/reason}` 数组 |

**AI 模型调用选择：**

**选 Promise.allSettled：**
- 多模型对比展示
- 单个失败不影响其他
- 需要知道每个模型结果

```typescript
const results = await Promise.allSettled([
    callOpenAI(message),
    callClaude(message)
]);
results.forEach((r, i) => {
    if (r.status === 'fulfilled') showModelResponse(i, r.value);
    else showModelError(i);
});
```

**选 Promise.all：**
- 数据聚合分析（需全部成功）
- 任一失败则整体失败

---

## 16.2 二面：深入原理与项目

### 16.2.1 React Concurrent Mode

#### 知识点详解

**核心 API：**

```tsx
// useTransition - 标记非紧急更新
const [isPending, startTransition] = useTransition();
startTransition(() => {
    setMessages(prev => [...prev, chunk]);  // 可被打断
});

// useDeferredValue - 延迟值
const deferredMessages = useDeferredValue(messages);
```

#### 真实面试题

**题目：React Concurrent Mode 对 AI 交互体验的帮助？**

**满分答案：**

**对 AI 交互的帮助：**

1. **可中断渲染**：AI 内容渲染可被用户输入/滚动打断
2. **优先级调度**：用户操作 > AI 内容更新
3. **useTransition**：AI 回复标记为非紧急

```tsx
function AIChat() {
    const [isPending, startTransition] = useTransition();
    
    // AI 流式输出
    startTransition(() => {
        setMessages(prev => [...prev, chunk]);
    });
    
    return (
        <>
            <Input />  {/* 始终响应 ✅ */}
            {isPending && <Loading />}
            <MessageList messages={messages} />  {/* 可被打断 ✅ */}
        </>
    );
}
```

---

### 16.2.2 长对话历史滚动优化

#### 知识点详解

```tsx
import { Virtuoso } from 'react-virtuoso';

// 虚拟滚动：只渲染可视区域
<Virtuoso
    data={messages}
    followOutput="smooth"
    itemContent={(i, msg) => <Message key={msg.id} {...msg} />}
/>
```

#### 真实面试题

**题目：数百条对话历史如何保证滚动平滑不卡顿？**

**满分答案：**

**优化策略：**

1. **虚拟滚动（Virtual Scrolling）**
   - 只渲染可视区域
   - `react-virtuoso` 或 `react-window`

2. **消息分片渲染**
   - 长消息分段处理
   - `requestIdleCallback` 分批执行

3. **防抖滚动**
   - 流式输出时防抖处理滚动

4. **Intersection Observer**
   - 懒加载非可视消息

5. **状态管理优化**
   - `memo`、`useMemo` 避免重渲染

---

### 16.2.3 Prompt Injection 防御

#### 知识点详解

**攻击类型：**
1. 直接注入：用户输入包含恶意指令
2. 间接注入：通过外部数据注入
3. 越狱攻击：绕过安全限制

**前端防御措施：**

```typescript
// 1. 输入过滤
function sanitizeInput(input: string): string {
    return input
        .replace(/[<>]/g, '')
        .replace(/["'`]/g, '\\$&')
        .trim();
}

// 2. 敏感词检测
const PATTERNS = [
    /ignore previous instructions/i,
    /disregard.*prompt/i,
    /DAN.*mode/i
];
function detectInjection(input: string): boolean {
    return PATTERNS.some(p => p.test(input));
}

// 3. 输入长度限制
const MAX_LENGTH = 4000;

// 4. 上下文隔离
function createContext(segments: Message[]) {
    return segments.map(s => `[${s.role}]: ${s.content}`).join('\n---\n');
}
```

#### 真实面试题

**题目：Prompt Injection 理解？前端能做哪些防御？**

**满分答案：**

**Prompt Injection：** 攻击者通过输入嵌入恶意指令，让 AI 执行非预期操作。

**前端防御措施：**

1. **输入过滤**：移除特殊字符，转义引号
2. **敏感词检测**：检测 "ignore instructions" 等模式
3. **长度限制**：防止超长输入
4. **上下文隔离**：明确标记用户输入边界
5. **后端配合**：后端深度检测，前端初筛

---

### 16.2.4 AI 应用 FCP 优化

#### 知识点详解

**FCP（First Contentful Paint）优化：**

```html
<!-- 1. 关键 CSS 内联 -->
<style>/* 首屏样式 */</style>

<!-- 2. 预加载关键资源 -->
<link rel="preload" as="image" href="logo.png" fetchpriority="high">

<!-- 3. 骨架屏 -->
<div class="skeleton"></div>

<!-- 4. 字体优化 -->
<link rel="preconnect" href="https://fonts.googleapis.com">
```

```typescript
// 路由懒加载
const ChatPage = lazy(() => import('./pages/ChatPage'));

// CDN 加速
// 使用 Edge Network（Cloudflare/Vercel Edge）
```

#### 真实面试题

**题目：如何优化 AI 应用的 FCP 和首屏加载速度？**

**满分答案：**

**优化策略：**

1. **首屏骨架屏**
   - AI 聊天加载动画
   - 减少白屏感知时间

2. **关键 CSS 内联**
   - 首屏样式直接内联到 HTML

3. **资源预加载**
   ```html
   <link rel="preload" as="image" href="avatar.png">
   <link rel="preconnect" href="https://api.example.com">
   ```

4. **字体优化**
   ```html
   <link rel="preconnect" href="https://fonts.googleapis.com">
   <style>font-display: swap;</style>
   ```

5. **代码分割**
   - 路由懒加载
   - 非首屏组件延迟加载

---

### 16.2.5 AI 对话 Context 管理

#### 知识点详解

**Context 管理策略：**

```typescript
// 1. 本地 Token 估算
function estimateTokens(text: string): number {
    return Math.ceil(text.length / 4);  // 粗略估算
}

// 2. 上下文窗口管理
class ContextManager {
    private messages: Message[] = [];
    private maxTokens: number;
    
    add(message: Message) {
        this.messages.push(message);
        while (this.getTotalTokens() > this.maxTokens) {
            this.messages.shift();  // 移除最早的
        }
    }
    
    getMessages(): Message[] {
        return this.messages;
    }
}

// 3. 滑动窗口策略
function trimContext(messages: Message[], maxTokens: number): Message[] {
    while (estimateTokens(messages) > maxTokens) {
        messages.splice(1, 1);  // 移除中间消息
    }
    return messages;
}
```

#### 真实面试题

**题目：如何管理 AI 对话 Context？前端参与 Token 计算吗？**

**满分答案：**

**Context 管理策略：**

1. **本地 Token 估算**
   - 按字符数粗略估算（中文约 1 字符 ≈ 1 Token）
   - 调用 Tokenizer 库精确计算

2. **滑动窗口**
   - 超出 maxTokens 时移除最早消息
   - 保留系统提示和最近对话

3. **摘要压缩**
   - 对长对话生成摘要替换历史

**前端参与 Token 计算：**

**前端做：**
- 粗略估算（提示用户输入过长）
- 显示 Token 消耗统计

**后端做：**
- 精确计算（模型相关）
- 强制截断（安全边界）

---

### 16.2.6 AI Agent 工作流可视化

#### 知识点详解

**技术栈选择：**

```typescript
// 节点图库
import { ReactFlow } from '@xyflow/react';
// 或
import { Dagre } from '@dagrejs/dagre';

// 自定义渲染
<ReactFlow nodes={nodes} edges={edges}>
    <AgentNode data={nodeData} />
    <StepEdge />
</ReactFlow>
```

**核心难点：**

1. **复杂布局算法**：自动排列节点
2. **实时状态同步**：Agent 执行进度
3. **撤销重做**：操作历史管理
4. **性能优化**：大量节点渲染

#### 真实面试题

**题目：AI Agent 工作流可视化用什么技术栈？核心难点？**

**满分答案：**

**技术栈：**

| 组件 | 技术选型 |
|------|---------|
| 节点图 | React Flow / X6 |
| 自定义渲染 | SVG / Canvas |
| 状态管理 | Zustand / Redux |
| 布局算法 | Dagre / ElkJS |

**核心难点：**

1. **布局算法**：DAG 自动布局
2. **实时同步**：WebSocket 状态更新
3. **撤销重做**：Command 模式
4. **性能**：大量节点虚拟化

---

### 16.2.7 前端 RAG 实现

#### 知识点详解

**RAG 流程：**

```
文档 → 解析 → 分块 → 向量化 → 索引 → 检索 → 增强 → 生成
```

**本地文档预处理：**

```typescript
// 1. 文件解析
import { PDFLoader } from 'langchain/document_loaders/fs/pdf';

// 2. 文本分块
import { RecursiveCharacterTextSplitter } from 'langchain/text_splitter';
const splitter = new RecursiveCharacterTextSplitter({
    chunkSize: 500,
    chunkOverlap: 50
});
const chunks = await splitter.splitDocuments(docs);

// 3. 向量化
import { OpenAIEmbeddings } from 'langchain/embeddings/openai';
const embeddings = new OpenAIEmbeddings();
const vectors = await embeddings.embedDocuments(chunks);

// 4. 本地索引
import { LocalIndex } from ' векторная-bd';
const index = new LocalIndex();
index.add(vectors, chunks);

// 5. 相似度检索
const results = await index.search(queryVector, k: 5);
```

#### 真实面试题

**题目：前端如何实现 RAG？本地文档预处理怎么做？**

**满分答案：**

**RAG 流程：**

```
文档 → 解析 → 分块 → 向量化 → 索引 → 检索 → 增强 → 生成
```

**前端实现：**

1. **文件解析**
   - PDF：pdf.js
   - Word：mammoth.js
   - 纯文本：直接读取

2. **文本分块**
   - 按字符数/句子数切分
   - 重叠处理保持上下文

3. **向量化**
   - 调用 Embedding API
   - Web Worker 后台处理

4. **本地索引**
   - IndexedDB 存储向量
   - Milvus Lite / Chroma

5. **检索**
   - 余弦相似度计算
   - Top-K 检索

---

### 16.2.8 缓解用户焦虑

#### 知识点详解

**交互手段：**

```tsx
// 1. 打字机效果（逐字显示）
const [displayText, setDisplayText] = useState('');

// 2. 思考过程可视化
{model === 'thinking' && <ThinkingIndicator />}

// 3. 进度预估
const [progress, setProgress] = useState(0);

// 4. 取消按钮
<button onClick={cancelRequest}>取消</button>

// 5. 部分结果预览
{partialResult && <Preview result={partialResult} />}
```

#### 真实面试题

**题目：AI 接口慢，除 Loading 外还有什么交互手段？**

**满分答案：**

**缓解焦虑的手段：**

| 手段 | 效果 |
|------|------|
| 打字机效果 | 逐字显示，感知进展 |
| 思考过程可视化 | 显示"AI 正在思考..." |
| 进度预估 | 显示"预计还需 X 秒" |
| 部分结果预览 | 提前看到内容 |
| 取消/暂停按钮 | 给予控制感 |
| 相关推荐 | 快速提供备选 |

**最佳实践：**

1. **先显示思考状态**
2. **流式输出**（有内容就显示）
3. **保留取消按钮**
4. **超时提示**（超过一定时间）

---

## 16.3 三面：架构与综合能力

### 16.3.1 多模态 AI 聊天组件库架构

#### 知识点详解

**组织架构：**

```
src/
├── components/
│   ├── ChatContainer/      # 容器
│   ├── MessageList/       # 消息列表
│   ├── MessageItem/       # 消息项
│   ├── InputArea/         # 输入区（含多模态）
│   └── MediaUpload/       # 媒体上传
├── hooks/
│   ├── useChat/          # 聊天逻辑
│   ├── useStream/        # 流式处理
│   └── useMedia/         # 媒体处理
├── providers/
│   └── AIProvider/       # AI 服务抽象
└── utils/
    └── format/           # 格式转换
```

#### 真实面试题

**题目：从零设计多模态 AI 聊天组件库，如何组织架构？**

**满分答案：**

**核心设计原则：**

1. **组件化**：每个功能独立组件
2. **可扩展**：方便新增模型/功能
3. **类型安全**：TypeScript 完整类型
4. **样式主题**：CSS 变量支持主题

**目录结构：**

```
components/
├── ChatContainer/     # 主容器，管理状态
├── MessageList/       # 虚拟滚动消息列表
├── MessageItem/       # 单条消息（含多模态）
├── InputArea/         # 输入区（文字+媒体）
├── MediaPreview/      # 图片/音频预览
└── ToolBar/           # 工具栏
```

---

### 16.3.2 前端自动化测试体系

#### 知识点详解

**测试分层：**

```typescript
// 1. 单元测试 - Jest + Testing Library
test('useChat returns messages', () => {
    const { result } = renderHook(() => useChat());
    expect(result.current.messages).toEqual([]);
});

// 2. 集成测试 - MSW 模拟 API
const server = setupServer(
    rest.post('/api/chat', (req, res, ctx) => {
        return res(ctx.json({ content: 'Hello' }));
    })
);

// 3. E2E 测试 - Playwright
test('chat sends message and receives response', async () => {
    await page.click('[data-testid=input]');
    await page.type('[data-testid=input]', 'Hello');
    await page.click('[data-testid=send]');
    await expect(page.locator('.message')).toBeVisible();
});
```

#### 真实面试题

**题目：AI 业务快速迭代中，如何建立前端自动化测试体系？**

**满分答案：**

**测试金字塔：**

```
        /\
       /E2E\       ← 少量，核心流程
      /------\
     /集成测试\    ← 中等，API 交互
    /----------\
   /  单元测试   \  ← 大量，业务逻辑
  /______________\
```

**工具选型：**

| 层级 | 工具 | 覆盖率目标 |
|------|------|-----------|
| 单元测试 | Jest + Testing Library | 70%+ |
| 集成测试 | MSW + Testing Library | 50%+ |
| E2E 测试 | Playwright | 核心场景 |

**AI 场景特殊测试：**
- 流式输出测试
- Token 计算准确性
- 错误处理测试

---

### 16.3.3 最难的 AI 前端问题

#### 知识点详解

**回答框架：**

1. **问题背景**：什么场景下遇到
2. **技术挑战**：为什么难
3. **解决方案**：如何解决
4. **经验教训**：学到什么

#### 真实面试题

**题目：过去一年遇到的最难 AI 前端问题？如何解决？**

**满分答案：**

**回答框架（STAR）：**

**Situation（背景）：**
> 在做一个长对话 AI 助手时，数百条消息后页面卡顿严重。

**Task（任务）：**
> 需要在保持功能完整的情况下，解决性能问题。

**Action（行动）：**
> 1. 定位瓶颈：Chrome DevTools Profiler 发现 DOM 节点过多
> 2. 引入虚拟滚动：`react-virtuoso`
> 3. 消息分片渲染：长消息分段处理
> 4. 状态优化：`React.memo` + `useMemo`

**Result（结果）：**
> 消息列表从卡顿到流畅，FPS 从 10 提升到 60。

**经验教训：**
> 虚拟滚动是长列表必备，性能优化要从数据层开始。

---

### 16.3.4 前端开发者角色转变

#### 知识点详解

**AI 时代前端角色变化：**

| 传统角色 | AI 时代角色 |
|---------|------------|
| 页面实现者 | AI 协同者 |
| 功能开发者 | 体验优化者 |
| 手动编码 | AI 辅助编程 |
| 单打独斗 | 全栈协作 |

#### 真实面试题

**题目：未来 1-2 年，前端角色在 AI 浪潮下如何转变？**

**满分答案：**

**核心转变：**

1. **从实现者到协同者**
   - 不再只是"写代码"
   - 需要理解 AI 能力，设计人机交互

2. **AI 辅助编程成标配**
   - Copilot / Cursor 等工具
   - 从"手写代码"到"审核 AI 代码"

3. **更注重产品思维**
   - 用户体验 > 代码实现
   - 需要理解 AI 局限，设计容错

4. **全栈能力要求提升**
   - AI 能力边界模糊前后端
   - 需要理解 AI API 和 Prompt 工程

5. **Prompt 工程成为新技能**
   - 好的 Prompt = 好的产品
   - 前端天然贴近用户，最懂交互 Prompt

---

### 16.3.5 音文字幕同步高亮

#### 知识点详解

**实现方案：**

```typescript
// 1. 解析字幕文件（SRT/VTT）
interface Subtitle {
    id: number;
    start: number;  // 秒
    end: number;
    text: string;
}

// 2. 音频同步
const audioRef = useRef<HTMLAudioElement>(null);

useEffect(() => {
    const audio = audioRef.current!;
    
    const handleTimeUpdate = () => {
        const currentTime = audio.currentTime;
        const active = subtitles.find(
            s => currentTime >= s.start && currentTime <= s.end
        );
        setActiveId(active?.id);
    };
    
    audio.addEventListener('timeupdate', handleTimeUpdate);
    return () => audio.removeEventListener('timeupdate', handleTimeUpdate);
}, [subtitles]);

// 3. 点击跳转
const handleSubtitleClick = (subtitle: Subtitle) => {
    audioRef.current!.currentTime = subtitle.start;
};
```

**样式：**

```css
.subtitle-item {
    cursor: pointer;
    padding: 4px 8px;
    border-radius: 4px;
    transition: background 0.2s;
}

.subtitle-item.active {
    background: #1890ff;
    color: white;
}
```

#### 真实面试题

**题目：播客 AI 场景，如何实现"音文字幕同步高亮"且"点击文字跳转音频"？**

**满分答案：**

**核心实现：**

```typescript
// 1. 音频时间同步
useEffect(() => {
    const audio = audioRef.current;
    const handleTimeUpdate = () => {
        const time = audio.currentTime;
        const active = subtitles.find(
            s => time >= s.start && time <= s.end
        );
        setActiveId(active?.id);
    };
    audio.addEventListener('timeupdate', handleTimeUpdate);
    return () => audio.removeEventListener('timeupdate', handleTimeUpdate);
}, [subtitles]);

// 2. 点击跳转
const handleClick = (subtitle: Subtitle) => {
    audioRef.current!.currentTime = subtitle.start;
    audioRef.current!.play();
};
```

**样式：**

```css
.subtitle.active {
    background: var(--primary-color);
    color: white;
}
```

**技术要点：**
1. `timeupdate` 事件驱动同步
2. `requestAnimationFrame` 优化性能
3. Subtitle 格式：SRT/VTT 解析
4. 点击事件设置 `audio.currentTime`
